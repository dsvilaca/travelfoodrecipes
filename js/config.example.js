/* Copia este ficheiro para config.js e cola a tua anon key.
   NUNCA uses a password da base de dados aqui. */
window.MARE_CONFIG = {
  supabaseUrl: "https://SEU_PROJECT.supabase.co",
  // Publishable key (sb_publishable_...) ou anon JWT (eyJ...)
  // NUNCA uses a secret key (sb_secret_...) nem a password da base de dados
  supabaseAnonKey: "sb_publishable_...",
};
